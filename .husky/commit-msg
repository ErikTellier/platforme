# Valide le message contre la convention (voir commitlint.config.mjs).
. ./.husky/commun.sh

set -e

pnpm exec commitlint --edit "$1"

# La preuve que la chaine a tourne — voir .husky/garde/poser-jeton.sh.
sh ./.husky/garde/poser-jeton.sh
